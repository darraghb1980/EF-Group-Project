/*using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace teamcanada.Logic
{
    public class CalculateTotalVotes
    {
        Results totalResult = new Results();
        viewDAL dbSelectVotes = new viewDAL();
        
        int totalVotes = 0;
        int rowVoteCount

        for (int i = 0; int <= rowCount; i++)                      // for loop
        {
            totalVotes = totalVotes + totalResult.NumVotes;       // keep adding amounts into totalAmount
        }

        return totalVotes;
    }
}
*/